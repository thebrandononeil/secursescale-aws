plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  tag:Project: SecureScale
  instance-state-name: running

hostnames:
  - instance-id

compose:
  ansible_host: instance_id
  ansible_connection: '"amazon.aws.aws_ssm"'
  ansible_aws_ssm_region: '"us-east-1"'
  ansible_aws_ssm_bucket_name: '"securescale-ansible-ssm-384e53d49d130baba47808ed97"'
